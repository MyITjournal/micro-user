# Server Configuration
PORT=8080
NODE_ENV=development

# Database Configuration (Option 1: Individual parameters)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=user_service

# Database Configuration (Option 2: Connection URL - used by Heroku)
# If DATABASE_URL is set, it takes precedence over individual parameters
# DATABASE_URL=postgresql://username:password@localhost:5432/user_service

# GraphQL Configuration
GRAPHQL_PLAYGROUND=true
