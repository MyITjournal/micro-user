name: Temporary Placeholder Workflow

on:
  push:
    branches: [ "*" ]
  pull_request:

jobs:
  noop:
    name: No-op Job
    runs-on: ubuntu-latest
    steps:
      - name: Do nothing
        run: echo "Temporary workflow to satisfy required checks."
